{% extends 'base_layout.html.twig' %}

{% form_theme form _self %}

{% block form_errors %}
    {% apply spaceless %}
        {% if errors is defined and errors|length > 0 %}
            <div class="alert alert-danger">
                <ul class="list-unstyled">
                    {% for error in errors %}
                        <li>{{ error.message }}</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}
    {% endapply %}
{% endblock form_errors %}

{% block title %}
    MIU
{% endblock %}

{% block maincontent %}
    <div id="uploader">
        <div class="row">
            <div class="col-md-2 mx-auto pt-4">
                {{ form_start(form) }}

                    <h1 class="text-center mrgb30">Login</h1>

                    <div class="mb-3 l-sign-mb-3">
                        {{ form_label(form.username, "Login", {'label_attr': {'class':'visually-hidden'} }) }}
                        {{ form_widget(form.username, {'value': lastUsername, 'attr':{'class': 'form-control', 'placeholder': 'Email / Login'}}) }}
                        {{ form_errors(form.username) }}
                    </div>

                    <div class="mb-3 l-sign-mb-3">
                        {{ form_label(form.password, "Password", {'label_attr': {'class':'visually-hidden'} }) }}
                        {{ form_widget(form.password, {'attr':{'class': 'form-control', 'placeholder': 'Password'}}) }}
                        {{ form_errors(form.password) }}
                    </div>

                    <div class="mb-3 l-sign-mb-3 form-check">
                        {{ form_widget(form._remember_me, {'attr': {'class': 'form-check-input'}}) }}
                        {{ form_label(form._remember_me, "Remember me for 30 days", {'label_attr': {'class': 'form-check-label'}}) }}
                    </div>

                    <div class="l-sign-inner">
                        {% if error is not null %}
                        <div class="alert alert-danger">
                            {{ "Login failed, check your info" }}
                        </div>
                        {% endif %}
                        <div class="l-sign-btn">
                            <button id="login-form-button" type="submit" class="btn btn-primary btn-wmid">Log in</button>
                        </div>

                    </div><!-- /.l-sign-inner -->

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
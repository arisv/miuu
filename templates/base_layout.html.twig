<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">

		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
			<link rel="icon" type="image/png" href="{{ asset('static/miu_favicon.png') }}">
		{% endblock %}
		<title>
			{% block title %}{% endblock %}
		</title>
		{% block meta %}{% endblock %}
	</head>
	<body class="glass-ui">
		<div class="app p-4">
			<div class="header">
				<div class="row align-items-start">
					<div id="header-home" class="col-md-2 col-sm-12 text-center">
						<a href="/"><img class="app-brand" src="{{ asset('static/logo.png')}}" alt="MIU"></a>
					</div>
                    <div class="d-md-none" id="sidebarShowBtn">
                    <i class="fa-solid fa-bars" aria-hidden="true"></i>
                    </div>
                    <div class="d-md-none" id="sidebarBackdrop" aria-hidden="true"></div>
                    <div class="d-md-none" id="sidebar" aria-hidden="true">
                        <div id="hamburger-userheader" class="">
                            <ul id="hamburger-profilebox" class="list-unstyled text-start list-group">
                                <li class="list-group-item"><span><img class="app-brand" src="{{ asset('static/logo.png')}}" alt="MIU"></span><span id="sidebarCloseBtn"><i class="fa-solid fa-xmark" aria-hidden="true"></i></span></li>
                                {% if is_granted('ROLE_USER') %}
                                <li class="list-group-item"><a href="{{ path('cabinet_home') }}"><i class="fa-solid fa-user fa-fw me-2" aria-hidden="true"></i>{{ app.user.login }}</a></li>
                                <li class="list-group-item"><a href="{{ path('cabinet_mypics') }}"><i class="fa-solid fa-images fa-fw me-2" aria-hidden="true"></i>Pictures</a></li>
                                <li class="list-group-item"><a href="{{ path('auth_logout') }}"><i class="fa-solid fa-right-from-bracket fa-fw me-2" aria-hidden="true"></i>Logout</a>
                                {% else %}
                                <li class="list-group-item"><a href="{{ path('auth_login') }}"><i class="fa-solid fa-right-to-bracket fa-fw me-2" aria-hidden="true"></i>Login</a></li>
                                {% if allow_registration %}
                                <li class="list-group-item"><a href="{{ path('auth_register') }}"><i class="fa-solid fa-user-plus fa-fw me-2" aria-hidden="true"></i>Sign up</a></li>
                                {% endif %}
                                {% endif %}
                                </li>
                            </ul>
                        </div>
                    </div>
					<div class="col text-end d-none d-md-block">
						{% if not is_granted('ROLE_USER') %}
							<div id="loginpanel" class="">
								<a href="{{ path('auth_login') }}"><i class="fa-solid fa-right-to-bracket fa-fw me-2" aria-hidden="true"></i>Login</a>
								{% if allow_registration %}
								or
								<a href="{{ path('auth_register') }}"><i class="fa-solid fa-user-plus fa-fw me-2" aria-hidden="true"></i>Sign up</a>
								{% endif %}
							</div>
						{% endif %}
						{% if is_granted('ROLE_USER') %}
							<div id="userheader" class="">
								<ul id="profilebox" class="list-unstyled list-inline text-start"><li class="list-inline-item"><a href="{{ path('cabinet_home') }}"><i class="fa-solid fa-user fa-fw me-2" aria-hidden="true"></i>{{ app.user.login }}</a></li><li class="list-inline-item"><a href="{{ path('cabinet_mypics') }}"><i class="fa-solid fa-images fa-fw me-2" aria-hidden="true"></i>Pictures</a></li><li class="list-inline-item"><a href="{{ path('auth_logout') }}"><i class="fa-solid fa-right-from-bracket fa-fw me-2" aria-hidden="true"></i>Logout</a>
									</li>
								</ul>
							</div>
						{% endif %}
					</div>
				</div>
			</div>
			{% if app.session.flashbag.has('global-success') or app.session.flashbag.has('global-danger') %}
				<div class="global-alerts">
					{% for flashMessage in app.session.flashbag.get('global-danger') %}
						<div class="alert alert-danger">
							<p class="text-center">{{ flashMessage }}</p>
						</div>
					{% endfor %}
					{% for flashMessage in app.session.flashbag.get('global-success') %}
						<div class="alert alert-success">
							<p class="text-center">{{ flashMessage }}</p>
						</div>
					{% endfor %}
				</div>
			{% endif %}
			<div id="mainblock" class="container-fluid"> {% block maincontent %}{% endblock %}
				</div>
			</div>

			<div class="footer container-fluid">
				<p id="footer-abuse" class="mx-auto text-center">
					Abuse email:
					<a href="mailto:abuse@onozuka.info">abuse@onozuka.info</a>
				</p>
			</div>
			{% block javascripts %}
				{{ encore_entry_script_tags('app') }}
			{% endblock %}
		</body>
	</html>
